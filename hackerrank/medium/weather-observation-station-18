SELECT ROUND((Q_C-P_A)+(Q_D-P_B),4)
FROM (SELECT MIN(LAT_N) AS P_A FROM STATION) AS MIN_LAT
JOIN (SELECT MAX(LAT_N) AS Q_C FROM STATION ) AS MAX_LAT
JOIN (SELECT MIN(LONG_W) AS P_B FROM STATION) AS MIN_LONG
JOIN (SELECT MAX(LONG_W) AS Q_D FROM STATION) AS MAX_LONG;